namespace Musio.Core.Shell;

/// <summary>
/// Which window Musio opens on launch.
/// </summary>
public enum StartupMode
{
    /// <summary>Open the compact Mini window docked at the top of the screen.</summary>
    Mini,

    /// <summary>Open the full app window.</summary>
    Full,
}
